# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    subscriptionId: '39871908-78ed-4ef9-adbf-cb5c701334e3'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'RG1'
    location: 'East US'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://raw.githubusercontent.com/de8899/azure/master/w001template.json'
    csmParametersFileLink: 'https://raw.githubusercontent.com/de8899/azure/master/parameters.json'
    deploymentMode: 'Incremental'
    deploymentName: 'deploy 1 vm'
steps:
- script: .\deploy.ps1
  displayName: 'build'
